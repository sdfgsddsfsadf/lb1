#include <stdio.h>
#include<math.h>
int main(){
float y;
const int a=-1;
const int b=12;
const int c=26;
y=(a+4*b-pow(c,2))/(2*a*b*c);
printf("y=(a+4*b-pow(c,2))/(2*a*b*c)");
printf("\n y=");
printf("%f",y);
return 0;
}